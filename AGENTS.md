# Instructions for AI Agents (E-STEP-TOOL)

Read FIRST:
- docs/design-rules.md
- docs/decision-log.md

Hard rules:
- Keep changes minimal and scoped.
- Never refactor unrelated code in the same change.
- Use partial updates; never overwrite with null/empty unless user explicitly cleared.
- If you need an exception/new convention, write a decision-log entry BEFORE coding.
